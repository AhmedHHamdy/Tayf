# المساهمة

*[In English](CONTRIBUTING.md)*

## التشغيل

محتاج Node 22 أو أحدث.

```bash
npm install
cp config.example.json config.json   # املا الموقع والإيميل والتوكن
npm start
```

`config.json` موجود في `.gitignore`. اعمل API Token من
[id.atlassian.com/manage-profile/security/api-tokens](https://id.atlassian.com/manage-profile/security/api-tokens).

لو الاتصال فشل، `npm run check-config` هيقولك إيه المشكلة في الملف من غير ما
يطبع التوكن نفسه.

```bash
npm test          # الاختبارات (node:test، من غير أي مكتبات)
npm run lint      # eslint
npm run dist      # حزم نسخة للمنصة الحالية
```

**مفيش خطوة بناء.** الواجهة موديولات ES عادية إلكترون بيحمّلها على طول، يعني اللي
بتعدّله هو اللي بيشتغل.

## قبل ما تغيّر أي حاجة

اقرا [docs/architecture.ar.md](docs/architecture.ar.md) — خمس دقايق، وبتشرح قاعدة
الطبقات اللي الكود كله قايم عليها.

وبعدين اقرا [docs/jira-quirks.ar.md](docs/jira-quirks.ar.md). الكود من غير كومنتات
عن قصد، فكل معرفة «ليه ده مكتوب بالشكل الغريب ده» موجودة هناك. كذا التفاف من
دول شكله إنه ينفع يتشال — ومينفعش.

## قواعد البيت

**مفيش كومنتات.** الكود المفروض يشرح نفسه بالأسماء والدوال الصغيرة. لو فيه حاجة
مستحيل تبقى واضحة — API بيتصرف غلط، أو مصيبة في منصة — مكانها
`docs/jira-quirks.ar.md` مش كومنت فوق السطر.

**الأسماء والتوثيق بالإنجليزي.** نصوص المستخدم فاضلة عربي دلوقتي؛ نصوص العملية
الرئيسية في `src/strings.js`.

**احترم قاعدة الطبقات.** `renderer → preload → main → app → providers/storage`.
وبالذات: مفيش بيانات على شكل جيرا (ADF، أو `customfield_*` خام، أو JSON بتاع
جيرا) تخرج من `src/providers/`، والواجهة ماتستوردش غير موديولاتها و`window.tayf`.

**الأخطاء بتشيل أكواد مش جُمل.** المزوّدين بيرموا `JiraError` ومعاه `code`. نص
العرض بيتجاب من `src/strings.js` عند حدود الـ IPC. ده اللي بيخلي الترجمة ملف واحد.

**اختبر الأجزاء الخالصة.** أي حاجة من غير إدخال وإخراج — تحليل، تحويل، تواريخ —
لازم يبقى ليها اختبارات. وأي حاجة بتلمس الشبكة غالباً لأ.

## إضافة حاجات

**مزوّد جديد (جيت هاب، Azure DevOps، …):** ضيف `src/providers/<name>/` بنفس واجهة
`JiraProvider`، وابنيه في `src/main/index.js → connectProvider`. مفيش حاجة فوق
طبقة المزوّدين المفروض تتغيّر. شوف
[ADR-0002](docs/adr/0002-provider-abstraction.md).

**دعم لينكس:** ضيف `src/main/platform/linux.js` بنفس شكل `windows.js` و`macos.js`،
واختاره في `platform/index.js`. الجزء المهم هو رجوع الفوكس — اقرا قسم ويندوز في
ملف المصايب الأول.

**شاشة جديدة:** اعمل `src/renderer/screens/<name>.js` بيصدّر
`{ name, enter, leave, render }`، وضيف الـ layout بتاعها في `SCREEN_PARTS` جوه
`chrome.js`، وسجّلها في `app.js`، وضيف معالج مفاتيح في `keyboard.js` لو محتاجة.

## طلبات الدمج

- شغلانة واحدة في كل PR.
- `npm test` و`npm run lint` يعدّوا.
- لو غيّرت سلوك، قول كده بصراحة — الأداة مستخدمة يومياً، وتغيير السلوك في
  السكوت أوحش من الباج.
- لو لفّيت حوالين حاجة جيرا أو نظام التشغيل بيعملها غلط، ضيفها في
  `docs/jira-quirks.ar.md` في نفس الـ PR. الملف ده هو المقصود.

## الإبلاغ عن مشاكل

اكتب نظام التشغيل، وهل ده تشغيل تطوير ولا نسخة محزومة، والجزء المهم من سجل
الأخطاء (أيقونة الساعة ← **سجل الأخطاء**). **عمرك ما تلصق التوكن بتاعك** —
اللوج مفيهوش، والـ issue كمان ماينفعش يبقى فيه.
